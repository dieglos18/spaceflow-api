services:
  postgres:
    image: postgres:16-alpine
    container_name: spaceflow-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: spaceflow
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
